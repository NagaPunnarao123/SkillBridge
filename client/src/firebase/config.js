// Standalone offline config — Firebase disconnected
export const app = {};
export const auth = {};
export const db = {};
export const storage = {};
export const analytics = null;
export default app;
